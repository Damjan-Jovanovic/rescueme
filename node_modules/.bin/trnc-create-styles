#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tailwind-react-native-classnames/dist/create-styles.js" "$@"
  ret=$?
else 
  node  "$basedir/../tailwind-react-native-classnames/dist/create-styles.js" "$@"
  ret=$?
fi
exit $ret
